services:
  - type: web
    name: cloudsentinel-insight-ai
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt && python database/init_db.py"
    startCommand: "gunicorn app:app --bind 0.0.0.0:$PORT"